package models

// Commit 代表一个 Git 提交
// 目前为空结构体，后续可以扩展添加字段（如 SHA, Author, Message, Date 等）
type Commit struct{}

// PullRequest 代表一个 Pull Request / Merge Request
// 目前为空结构体，后续可以扩展添加字段（如 Number, Title, State, CreatedAt 等）
type PullRequest struct{}
